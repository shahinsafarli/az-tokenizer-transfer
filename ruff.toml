line-length = 100
target-version = "py310"

extend-exclude = [
    "src",
    "tests",
    "notebooks",
    "configs",
    "results",
    "evidence",
]

[lint]
select = ["E", "F", "W", "I", "UP", "B"]
ignore = ["E501", "B008"]
