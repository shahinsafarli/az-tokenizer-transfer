# Report

`report.tex` integrates ten section files: two assigned to each member. The section files, claim fragments, and bibliography fragments are draft slots until their registered final delivery PRs fill them.

The five bibliography fragments are referenced directly from `report.tex`. `refs.bib` is reserved for shared references. The final report PDF is committed only during the final delivery step.
