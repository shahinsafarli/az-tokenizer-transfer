# =====================================================================
#  !! DOES NOT RESOLVE AS WRITTEN — read this before using it (2026-09-08) !!
#
#  `fsspec==2026.7.0` below conflicts with `datasets==2.21.0`, which requires
#  `fsspec[http]<=2024.6.1`. pip exits with ResolutionImpossible, and under
#  `set -euo pipefail` that kills any launcher that installs this file — which
#  is why scripts/vastai_run_full.sh, scripts/vastai_stage1.sh and
#  scripts/a100_bootstrap.sh no longer do.
#
#  What the experiments actually ran on: the container image's stack —
#  torch 2.11.0+cu128, transformers 4.57.6 — recorded in
#  results/environment_provenance.json. The torch==2.4.1+cu121 pin below is
#  OLDER than that.
#
#  The two constraints the code genuinely has, both asserted at launch:
#    * transformers 4.x  (5.x removed TrainingArguments(warmup_ratio=...))
#    * a CUDA torch with kernels for the rented card
#
#  TO FIX FOR THE TAG: regenerate this file and requirements.txt from the
#  completed run's `pip freeze`. Until then treat the block below as a RECORD
#  of the 4060 dev venv, not as an installer.
# =====================================================================
# requirements.lock — EXACT versions resolved in the working .venv on the
# RTX 4060 development machine, 2026-09-04, during the GPU-stall diagnostic
# (see HANDOFF.md §12, configs/FROZEN.md "Amendments").
#
# Install with:
#   pip install -r requirements.lock \
#       --extra-index-url https://download.pytorch.org/whl/cu121
#
# Deliberately NOT --no-deps: on Linux the cu121 torch wheel additionally
# pulls in separate nvidia-*-cu12 runtime packages (cublas, cudnn, cufft,
# curand, cusolver, cusparse, nccl, nvtx, nvrtc, cuda-runtime, cuda-cupti)
# that this lock file does not list, because on this Windows dev machine
# those CUDA libraries ship bundled inside the torch wheel itself instead of
# as separate pip packages — there is nothing to freeze for them here. Let
# pip resolve them; only the exact top-level versions below are pinned.
#
# torch MUST resolve to the +cu121 build below, not the plain PyPI (CPU-only)
# build of the same version number — the +cu121 suffix is not on PyPI itself,
# only on the PyTorch wheel index, hence --extra-index-url above.
# torch.version.cuda == 12.1, bundled cuDNN == 90100 (9.1.0) on this machine
# — confirm the Linux install resolves the same torch.backends.cudnn.version()
# in docs/A100_SETUP.md's verification step.
# Host CUDA driver on the 4060 dev machine: 566.24 (informational only —
# driver is a host-level install, not a pip package, and A100 hosts commonly
# run a different, newer driver; what must match across machines is the
# torch/transformers/tokenizers/numpy/scipy/scikit-learn stack below).
# Python == 3.11.9 on the 4060 dev machine.
#
# Do not resolve this stack fresh on the A100 host (see docs/A100_SETUP.md,
# Step 2) — version drift between machines would invalidate cross-machine
# comparability of anything measured on either GPU.

torch==2.4.1+cu121
transformers==4.44.2
tokenizers==0.19.1
numpy==1.26.4
scipy==1.14.1
scikit-learn==1.5.2

# ---- transitive / task dependencies, pinned for the same reason ----
accelerate==0.34.2
datasets==2.21.0
sentencepiece==0.2.0
pandas==2.2.3
matplotlib==3.9.2
PyYAML==6.0.2
pytest==8.3.3
safetensors==0.8.0
huggingface_hub==0.36.2
fsspec==2026.7.0
filelock==3.32.3
regex==2026.9.3
requests==2.34.2
sacremoses==0.2.0
tqdm==4.70.0
typing_extensions==4.16.0
sympy==1.14.0
mpmath==1.3.0
networkx==3.6.1
Jinja2==3.1.6
MarkupSafe==3.0.3
packaging==26.3
pillow==12.3.0
pyarrow==25.0.1
dill==0.3.8
multiprocess==0.70.16
xxhash==4.0.1
aiohttp==3.14.3
aiohappyeyeballs==2.7.1
aiosignal==1.4.0
frozenlist==1.8.0
multidict==6.7.1
propcache==0.5.2
yarl==1.24.5
attrs==26.1.0
idna==3.19
urllib3==2.7.0
certifi==2026.7.22
charset-normalizer==3.5.1
click==8.5.0
cloudpickle==3.1.2
colorama==0.4.6
contourpy==1.3.3
cycler==0.12.1
fonttools==4.64.0
iniconfig==2.3.0
joblib==1.6.0
kiwisolver==1.5.1
pluggy==1.6.0
psutil==7.2.2
pyparsing==3.3.2
python-dateutil==2.9.0.post0
pytz==2026.3.post1
six==1.17.0
threadpoolctl==3.6.0
tzdata==2026.3
