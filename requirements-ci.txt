# PR01 scope/hygiene uses only the Python standard library.
# These dependencies are installed only once the full source suite is ready
# (PR09). CPU torch is deliberate; historical training pins remain untouched.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.4.1+cpu
transformers==4.44.2
tokenizers==0.19.1
datasets==2.21.0
sentencepiece==0.2.0
accelerate==0.34.2
scikit-learn==1.5.2
scipy==1.14.1
numpy==1.26.4
pandas==2.2.3
matplotlib==3.9.2
PyYAML==6.0.2
pytest==8.3.3
