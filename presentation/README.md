# Presentation

`presentation.tex` is the integration skeleton. Each file in `sections/` reserves four frames for its assigned member. Draft slots remain visibly marked until the corresponding final delivery PR replaces them.

Build from this directory with `pdflatex presentation.tex`. The final PDF is committed only during the final delivery step.
